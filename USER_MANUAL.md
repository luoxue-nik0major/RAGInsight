# RAGInsight 使用说明书

> 版本：1.0 | 最后更新：2026-05-21

---

## 目录

1. [系统概述](#1-系统概述)
2. [安装与部署](#2-安装与部署)
3. [配置说明](#3-配置说明)
4. [快速入门](#4-快速入门)
5. [界面布局](#5-界面布局)
6. [功能详解](#6-功能详解)
   - 6.1 [执行查询](#61-执行查询)
   - 6.2 [流程图可视化](#62-流程图可视化)
   - 6.3 [详情面板](#63-详情面板)
   - 6.4 [根因分析](#64-根因分析)
   - 6.5 [策略推荐与对比](#65-策略推荐与对比)
   - 6.6 [扰动分析与 What-If](#66-扰动分析与-what-if)
   - 6.7 [因果归因分析](#67-因果归因分析)
   - 6.8 [批量实验](#68-批量实验)
   - 6.9 [双向联动高亮](#69-双向联动高亮)
7. [API 接口参考](#7-api-接口参考)
8. [常见问题](#8-常见问题)

---

## 1. 系统概述

RAGInsight 是一个 **RAG（检索增强生成）系统的可视化诊断工具**。它能够：

- 对用户查询执行完整的 RAG 流水线（检索→上下文构建→答案生成）
- 实时可视化每一步的执行过程和耗时
- 自动检测 6 种故障类型并给出修复建议
- 分析每个检索片段的实际重要性（扰动分析）
- 通过反事实干预定位故障根因（因果归因）
- 智能推荐最优检索策略（启发式/学习型路由器）
- 支持批量实验，自动计算故障检测和策略对比指标
- 导出 PDF 报告和论文级图表

**三种检索策略**：

| 策略 | 图标 | 说明 | 适用场景 |
|------|------|------|---------|
| 向量检索 (vector) | search | 纯语义向量相似度搜索 | 概念性、语义丰富的查询 |
| 混合检索 (hybrid) | git-merge | 向量+BM25关键词 RRF融合 | 需要精确匹配+语义理解的查询 |
| 图检索 (graph) | share-2 | 知识图谱多跳BFS遍历 | 多实体关系推理查询 |

---

## 2. 安装与部署

### 2.1 环境要求

| 组件 | 最低要求 |
|------|---------|
| Python | 3.10+ |
| Node.js | 18+ |
| 内存 | 8GB |
| 磁盘 | 2GB（含嵌入模型） |
| 网络 | 需访问 DeepSeek API |

### 2.2 Docker 一键部署（推荐）

```bash
# 1. 克隆项目
git clone <your-repo-url>
cd raginsight

# 2. 配置 API Key
cp .env.docker .env
# 编辑 .env，填入 RAGINSIGHT_DEEPSEEK_API_KEY=sk-your-key

# 3. 启动
docker-compose up -d

# 4. 访问
# 前端: http://localhost:3000
# 后端: http://localhost:8000
```

### 2.3 手动安装

```bash
# 1. 后端
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
# 编辑 .env，填入 API Key
python -m alembic upgrade head
uvicorn app.main:app --reload --port 8000

# 2. 前端（新终端）
cd frontend
npm install
npm run dev
```

---

## 3. 配置说明

编辑 `backend/.env` 文件：

```env
# 必填：DeepSeek API 密钥
RAGINSIGHT_DEEPSEEK_API_KEY=sk-your-api-key-here

# 可选配置
RAGINSIGHT_DEEPSEEK_BASE_URL=https://api.deepseek.com
RAGINSIGHT_DEEPSEEK_MODEL=deepseek-v4-flash
RAGINSIGHT_DATABASE_URL=sqlite+aiosqlite:///./raginsight.db
RAGINSIGHT_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# 路由器模式：heuristic（启发式）或 learned（学习型）
RAGINSIGHT_ROUTER_MODE=heuristic
```

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `RAGINSIGHT_DEEPSEEK_API_KEY` | **必填** | DeepSeek API 密钥 |
| `RAGINSIGHT_DEEPSEEK_MODEL` | `deepseek-v4-flash` | 使用的 LLM 模型 |
| `RAGINSIGHT_EMBEDDING_MODEL` | `BAAI/bge-small-zh-v1.5` | 嵌入模型（中英文通用） |
| `RAGINSIGHT_ROUTER_MODE` | `heuristic` | 路由模式：`heuristic` 或 `learned` |

---

## 4. 快速入门

### 第一步：打开界面

浏览器访问 `http://localhost:3000`，你将看到三栏布局界面。

### 第二步：执行首次查询

1. 在左侧 **查询面板** 的输入框中输入问题，例如：
   ```
   What is artificial intelligence?
   ```
2. 在策略下拉菜单中选择 **向量检索**
3. 点击 **执行查询** 按钮
4. 观察中间区域的流程图逐步构建，右侧面板显示详情

### 第三步：查看结果

查询完成后：
- **中间**：完整的执行流程图（查询解析→检索→上下文构建→答案生成）
- **右侧**：最终答案、质量指标、检索片段
- **顶部**：如果检测到问题，会显示根因分析横幅

---

## 5. 界面布局

```
┌──────────────────────────────────────────────────────────────┐
│  根因分析横幅（仅在检测到故障时显示）                          │
├──────────────────────────────────────────────────────────────┤
│  策略推荐横幅（复杂度分数 + 推荐策略 + 策略切换）              │
├────────────┬──────────────────────────┬───────────────────────┤
│  左侧面板   │      中间面板              │     右侧面板          │
│  (320px)   │      (自适应)             │     (384px)          │
│            │                          │                      │
│  [查询|实验]│   流程图 / 对比视图        │   步骤详情 /          │
│  标签页切换 │                          │   会话概览            │
│            │   - 步骤节点              │                      │
│  查询输入框 │   - 片段节点              │   - 最终答案          │
│  策略选择器 │   - 播放控制              │   - 质量指标          │
│  历史记录   │   - 缩放/平移             │   - 扰动分析          │
│            │                          │   - What-If 分析      │
│  实验面板   │                          │   - 告警列表          │
│  数据集/结果│                          │   - 因果归因图         │
│  /指标     │                          │                      │
└────────────┴──────────────────────────┴───────────────────────┘
```

---

## 6. 功能详解

### 6.1 执行查询

**位置**：左侧面板 → 查询标签页

**操作步骤**：

1. 在文本框中输入查询（支持中英文）
2. 从下拉菜单选择检索策略（vector / hybrid / graph）
3. 点击 **分析复杂度**（可选）查看查询的复杂度分析雷达图
4. 点击 **执行查询** 开始 RAG 流水线

**流水线阶段**：

| 阶段 | 步骤类型 | 说明 |
|------|---------|------|
| 1 | query_parse | 实体提取、关键词分词 |
| 2 | vector_retrieve | 执行检索策略，返回 top-5 片段 |
| 3 | context_build | 拼接上下文，检查长度 |
| 4 | answer_generate | 调用 LLM 生成带引用的答案 |
| 5 | answer_evaluate | 评估答案忠实度和相关性（本地计算） |
| 6 | root_cause | 分析是否存在故障及根因 |

**会话管理**：

- 每次查询自动创建一个会话（Session）
- 历史会话列表显示在查询面板下方
- 点击历史会话可重新加载查看
- 会话状态：running（执行中）、completed（完成）、failed（失败）

---

### 6.2 流程图可视化

**位置**：中间面板

流程图以 ReactFlow 有向图展示 RAG 流水线的每一步。

**节点类型**：

| 颜色 | 步骤 | 显示信息 |
|------|------|---------|
| 紫色 | 查询解析 | 查询文本 |
| 蓝色 | 向量检索 | 策略名、片段数、质量分数 |
| 紫色 | 重排序（预留） | — |
| 绿色 | 上下文构建 | 片段数量、上下文长度 |
| 琥珀色 | 答案生成 | 答案质量、耗时 |

**片段（Chunk）节点**：显示在检索步骤右侧，节点大小和不透明度表示重要性分数。

**播放控制**：

流程图支持时间轴回放，逐步展示流水线执行过程：

| 按钮 | 功能 |
|------|------|
| ⏮ | 重置到开始 |
| ◀ | 上一步 |
| ▶/⏸ | 播放/暂停（自动每 800ms 前进一步） |
| ⏭ | 下一步 |

进度条显示当前播放位置。

**慢步骤高亮**：耗时超过 2 秒的节点会有红色脉冲边框动画。

**缩放与平移**：支持鼠标滚轮缩放、拖拽平移，右下角有小地图导航。

---

### 6.3 详情面板

**位置**：右侧面板

详情面板有两种显示模式：

#### 模式一：步骤详情（点击流程图节点时）

- 步骤类型名称
- 质量分数（进度条）
- 执行耗时
- 输入数据（JSON）
- 输出数据（JSON）
- 关联的检索片段列表
- 该步骤触发的告警

#### 模式二：会话概览（未选中节点时）

包含以下子区域：

**最终答案**：
- 逐句展示，带引用标记
- 绿色背景 = 有检索片段支撑
- 红色背景 = 无支撑（可能的幻觉）
- 鼠标悬停句子 → 流程图中对应片段高亮

**质量指标**：
- 相关性 (Relevance)
- 覆盖率 (Coverage)
- 多样性 (Diversity)
- 综合分数 (Combined)
- 答案忠实度 (Faithfulness)
- 答案相关性 (Answer Relevance)

**告警列表**：按严重程度排序（Error > Warning），每条告警包含类型、消息、建议。

---

### 6.4 根因分析

**位置**：页面顶部横幅

当流水线检测到故障时，根因分析器会自动诊断并显示彩色横幅：

| 根因类型 | 颜色 | 触发条件 |
|---------|------|---------|
| 检索失败 | 红色 | 相关性低 + 覆盖率低 |
| 知识不完整 | 琥珀色 | 检索结果为空 |
| 幻觉风险 | 紫色 | 答案引用不存在的片段 |
| 策略不匹配 | 蓝色 | 相关性低或上下文过长 |
| 无问题 | 绿色 | 未检测到故障 |

横幅显示：
- 根因类型（中英文标签）
- 严重程度
- 错误/警告数量
- 详细解释
- 修复建议列表（2-3 条）

---

### 6.5 策略推荐与对比

**位置**：页面顶部（根因分析下方）

**策略推荐横幅**显示：
- 查询复杂度分数（大号数字，0-1）
- 推荐策略（带颜色徽章）
- 推荐理由（中文解释）
- 备选策略按钮

**手动切换策略**：点击策略按钮可切换到其他策略并重新执行查询。

**策略对比**：
1. 先执行一次查询（例如用 vector）
2. 点击 **策略对比** 按钮
3. 系统会用相同查询、不同策略再执行一次
4. 中间面板切换为**并排对比视图**，左右对比两个策略的流程图

---

### 6.6 扰动分析与 What-If

**位置**：右侧面板 → 会话概览

#### 扰动分析（Perturbation Analysis）

评估每个检索片段对最终答案的**实际重要性**。

**操作**：
1. 执行一次查询（需有检索结果）
2. 在右侧面板找到"扰动分析"区域
3. 点击 **触发扰动分析** 按钮
4. 系统在后台执行两阶段分析：
   - **阶段一（近似）**：对所有片段用嵌入相似度快速估算重要性
   - **阶段二（精确）**：对 Top-3 片段做 Leave-One-Out，重新调用 LLM 生成答案

**结果解读**：
- 每个片段显示 `importance_score`（0-1）
- 橙色 = 高重要性（> 0.65）
- 蓝色 = 中等（0.35-0.65）
- 灰色 = 低（< 0.35）
- `is_approximate: false` = 经过 LLM 精确验证
- `is_approximate: true` = 仅嵌入估算

#### What-If 分析

验证"如果移除了某些片段，答案会怎样变化"。

**操作**：
1. 在 What-If 区域勾选要移除的片段
2. 点击 **运行 What-If**
3. 系统重新生成答案并显示：
   - 原始答案 vs 新答案（并排对比）
   - 语义相似度分数

---

### 6.7 因果归因分析

**位置**：右侧面板 → 因果归因标签页

这是 RAGInsight 的核心创新功能，从"检测到问题"升级到"**哪个组件导致了问题**"。

**因果 DAG 结构**：
```
Query → Strategy → Retrieval → Context → LLM → Quality
```

**操作**：
1. 执行一次查询
2. 在右侧面板找到"因果归因"区域
3. 点击 **Run Attribution Analysis** 按钮
4. 系统依次执行所有组件级别的反事实干预：
   - 切换检索策略
   - 改变 top-K 值
   - 移除个别片段
   - 压缩上下文窗口
5. 查看结果：
   - **因果图**：SVG 节点图，节点大小 = 归因分数
   - **组件归因**：各组件对答案质量的贡献百分比
   - **Top Contributors**：影响最大的干预措施排名

**两阶段成本优化**：
- 阶段一用嵌入近似覆盖所有干预（免费，< 2 秒）
- 阶段二用 LLM 精确验证 Top-3（调用 API，约 15 秒）

---

### 6.8 批量实验

**位置**：左侧面板 → 实验标签页

#### 标签页 1：数据集

浏览测试查询列表：
- 查询 ID、文本、类别、预期深度、推荐策略、预期告警
- 支持 squad（英文 225 条）和 chinese（中文 50 条）两个数据集

#### 标签页 2：结果

查看实验运行结果：
- 每条结果包含 query_id、策略、状态、实际告警
- 支持导出单个会话的完整 JSON

#### 标签页 3：实验指标

**实验一：故障检测准确率**

| 指标 | 说明 |
|------|------|
| Macro Precision | 各类别精确率的算术平均 |
| Macro Recall | 各类别召回率的算术平均 |
| Macro F1 | 各类别 F1 的算术平均 |
| Per-Type 表格 | 每种告警类型的 P/R/F1/TP/FP/FN |

**实验二：跨策略对比**

| 指标 | 说明 |
|------|------|
| avg_relevance | 平均检索相关性 |
| avg_coverage | 平均查询覆盖率 |
| avg_diversity | 平均片段多样性 |
| avg_answer_quality | 平均答案质量 |
| avg_faithfulness | 平均答案忠实度 |
| avg_answer_relevance | 平均答案相关性 |
| avg_duration_ms | 平均耗时 |

**PDF 报告导出**：
1. 在实验指标标签页点击 **导出 PDF**
2. 生成包含封面页 + 指标表格 + 策略对比截图的 PDF 文件

**Markdown 报告导出**：
1. 点击 **导出报告** 按钮
2. 下载 Markdown 格式的实验报告

---

### 6.9 双向联动高亮

**功能**：在答案句子和检索片段之间建立可视化关联。

**方向一：句子 → 片段**
- 在右侧面板的答案区域，鼠标悬停到某个答案句子上
- 中间流程图中，支撑该句子的片段节点会：
  - 边框变为蓝色发光
  - 放大 1.05 倍
  - 显示蓝色阴影

**方向二：片段 → 句子**
- 在中间流程图中，鼠标悬停到某个片段节点上
- 右侧面板中，该片段支撑的答案句子会：
  - 显示 indigo 色高亮环
  - 放大 1.05 倍

---

## 7. API 接口参考

### 查询与会话

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/query` | 执行 RAG 查询（SSE 流式返回） |
| GET | `/api/sessions` | 获取会话历史列表 |
| GET | `/api/sessions/{id}` | 获取会话详情 |
| POST | `/api/analyze-complexity` | 分析查询复杂度 |

### 扰动与因果分析

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/sessions/{id}/perturbation` | 触发扰动分析 |
| GET | `/api/tasks/{task_id}/status` | 查询任务状态 |
| POST | `/api/sessions/{id}/perturbation/what-if` | 执行 What-If 分析 |
| POST | `/api/sessions/{id}/causal-attribution` | 触发因果归因分析 |
| GET | `/api/sessions/{id}/causal-attribution` | 获取因果归因结果 |

### 实验

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/experiments/run?dataset=squad` | 运行批量实验 |
| POST | `/api/experiments/full-grid?dataset=squad` | 运行全网格实验（所有策略） |
| GET | `/api/experiments/status` | 获取实验进度 |
| GET | `/api/experiments/results` | 获取实验原始结果 |
| GET | `/api/experiments/metrics` | 获取聚合指标 |
| GET | `/api/experiments/report` | 获取 Markdown 报告 |
| GET | `/api/experiments/router-training-data` | 获取路由器训练数据 |
| POST | `/api/experiments/train-router` | 训练学习型路由器 |
| GET | `/api/experiments/router-comparison` | 获取路由器对比结果 |

---

## 8. 常见问题

### Q1: 查询返回"未能在知识库中找到相关信息"

**原因**：检索结果为空（empty_results）。

**解决方案**：
- 检查查询是否与知识库主题相关（当前英文库为 Super Bowl 50，中文库为唐诗宋词）
- 尝试切换到混合检索（hybrid）策略
- 尝试改写查询，使用更通用的关键词

### Q2: 图检索策略不生效

**原因**：`knowledge_graph.json` 文件不存在或图结构不完整。

**说明**：图检索策略在找不到知识图谱时会自动降级为向量检索。如需使用图检索，需要确保 `backend/app/data/knowledge_graph.json` 文件存在且包含正确的实体-文档关联。

### Q3: 扰动分析执行很慢

**原因**：精确阶段（Stage 2）需要为 Top-3 片段各调用一次 LLM API。

**说明**：
- 近似阶段（Stage 1）只需 2-3 秒
- 精确阶段每次 LLM 调用约 5-10 秒
- 总计约 15-30 秒，属于正常范围

### Q4: 学习型路由器准确率低于启发式路由器

**原因**：训练数据不足或特征信息量不够。

**解决方案**：
- 运行完整全网格实验收集更多训练数据
- 检查 `RAGINSIGHT_ROUTER_MODE` 配置，可随时切回 `heuristic`
- 查看路由器对比结果了解具体差距

### Q5: 前端显示"连接错误"

**排查步骤**：
1. 确认后端正在运行：访问 `http://localhost:8000/docs` 查看 FastAPI 文档
2. 检查前端代理配置：`frontend/vite.config.ts` 中 `/api` 代理目标是否为 `http://localhost:8000`
3. 查看浏览器开发者工具的 Network 标签页定位具体失败请求

### Q6: 如何切换中英文？

系统自动检测查询语言（中文汉字占比 > 30% 判定为中文），无需手动切换。检索时会自动路由到对应的 ChromaDB 集合（chinese_poetry / nq_documents）。

### Q7: 数据库在哪里？如何重置？

数据库文件位置：`backend/raginsight.db`（SQLite）

重置方法：
```bash
cd backend
rm raginsight.db          # 删除数据库
python -m alembic upgrade head  # 重新建表
```

### Q8: ChromaDB 向量库在哪里？

默认路径：`backend/chroma_db/`

包含两个集合：
- `nq_documents`：英文文档向量
- `chinese_poetry`：中文文档向量

---

## 附录：快捷键参考

| 操作 | 快捷键/方式 |
|------|-----------|
| 执行查询 | 在输入框按 Enter（需先输入内容） |
| 缩放流程图 | 鼠标滚轮 |
| 平移流程图 | 鼠标拖拽空白区域 |
| 重置流程图视图 | 点击右下角小地图的"适应视图"按钮 |
| 播放/暂停回放 | 点击播放按钮或按 Space |
| 选中步骤查看详情 | 点击流程图中任意步骤节点 |

---

> 如有其他问题或功能建议，请提交 Issue 到项目仓库。
