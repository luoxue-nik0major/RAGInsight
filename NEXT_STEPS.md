# RAGInsight — 下一步提高方向

基于对项目当前代码、架构与已知限制的全面检查，以下从 **检索内核**、**工程化**、**可视化**、**评估体系**、**多语言** 五个维度给出可落地的改进方向。按优先级排序，每个方向标注预估工作量与关键收益。

---

## 🔴 P0 — 检索内核增强（当前最短板）

### 1. 真正实现混合检索（Hybrid Retrieval）
**现状**：`hybrid` 策略目前只是框架占位，实际仍走纯向量检索逻辑。  
**改进方案**：
- 引入稀疏向量 / BM25：使用 `rank-bm25` 或 `Pyserini` 在入库时对文档做 tokenization 并构建倒排索引
- 融合排序：向量相似度分数 + BM25 分数做加权融合（如 `score = 0.7 * dense + 0.3 * sparse`）
- 在 `retriever.py` 中实现 `HybridRetriever`，与 `VectorRetriever` 统一接口

**收益**：解决短关键词查询召回差的问题（如专有名词、缩写），策略推荐不再停留在"概念演示"。

**预估工作量**：2~3 天

### 2. 图检索（GraphRAG）原型验证
**现状**：`graph` 策略完全未实现。  
**改进方案**：
- 利用已有调研资料（`检索器方法创新/` 目录下的 GraphRAG 论文）
- 轻量实现：基于 `networkx` 构建本地实体关系图，节点为文档 Chunk，边为共现实体 / 语义相似度
- 多跳检索：从种子 Chunk 出发，按实体共现做 BFS 扩展，取 2-hop 内的邻居作为补充上下文
- 不需要外部图数据库，降低部署成本

**收益**：让"多跳型 / 比较型"复杂查询真正有能力被解答，策略推荐的 `graph` 选项不再为空壳。

**预估工作量**：4~5 天

---

## 🟠 P1 — 工程化与生产化

### 3. 持久化异步任务队列
**现状**：扰动分析与批量实验使用内存信号量（`asyncio.Semaphore`），服务重启任务状态全丢。  
**改进方案**：
- 引入轻量级任务队列：`celery` + `redis` 或 `arq`（纯 Python，与 FastAPI async 生态更契合）
- 任务状态写入数据库新增 `tasks` 表，支持断点续传与历史回溯
- 前端轮询改为 WebSocket 推送进度，减少无效请求

**收益**：支持长时间运行的批量实验不中断，生产环境可预期。

**预估工作量**：2~3 天

### 4. 引入缓存层
**现状**：相同查询每次都重新走 embedding + 检索 + LLM，成本高且慢。  
**改进方案**：
- **检索缓存**：查询向量 → 结果列表做 LRU 缓存（如 `cachetools` + TTL）
- **答案缓存**：对简单事实型查询（复杂度 < 0.3），直接缓存答案，命中时秒回
- **Embedding 缓存**：对高频查询文本预计算 embedding，避免重复 ONNX 推理

**收益**：降低 DeepSeek API 调用费用，提升响应速度 50%+。

**预估工作量**：1~2 天

### 5. 配置管理与环境隔离
**现状**：`.env` 直接含 API Key，配置散落在多个文件中。  
**改进方案**：
- 统一使用 `pydantic-settings` 做配置校验与分层（dev / test / prod）
- 敏感信息（API Key）通过环境变量注入，`.env` 加入 `.gitignore` 并提供一个 `.env.example`
- 区分 `chroma_db`（生产）与 `chroma_db_test`（测试），已在代码中部分体现，需彻底隔离

**收益**：避免密钥泄露，支持测试/生产双环境切换。

**预估工作量**：0.5 天

---

## 🟡 P2 — 中文与多语言支持

### 6. 中文查询解析与复杂度分析
**现状**：复杂度分析的实体识别（`[A-Z][a-zA-Z]{2,}`）和关系词库完全面向英文，中文查询会严重失效。  
**改进方案**：
- 实体识别：接入轻量级中文 NER，如 `jieba` 词性标注（提取 `nr` 人名、`ns` 地名、`nt` 机构名）或 `HanLP` 轻量版
- 关系词库：扩展一套中文关系词（如"比较"、"差异"、"原因"、"影响"、"导致"）
- 句子分割：支持中英文混合标点（`。！？.!?`）
- 问题类型：增加"定义型"中文关键词（"什么是"、"介绍一下"、"含义"）

**收益**：项目根目录已有 `data/chinese-poetry`，说明有中文场景诉求；中文支持后才能做真正的中文知识库实验。

**预估工作量**：2~3 天

### 7. 中文 Embedding 与分词
**现状**：使用 `all-MiniLM-L6-v2`（纯英文模型），中文语义表示能力弱。  
**改进方案**：
- 更换为双语/多语言 Embedding：`bge-m3`、`paraphrase-multilingual-MiniLM-L12-v2` 或 `m3e-base`
- 这些模型同样支持 ONNX 导出，可通过 `onnxruntime` 本地推理，不增加 API 成本
- 分词器需同步更换，确保中文短文本的召回率

**收益**：中文知识库检索质量质的提升，为多语言扩展打下基础。

**预估工作量**：1~2 天

---

## 🟢 P3 — 可视化与交互增强

### 8. 流程图时间轴回放
**现状**：流程图只展示最终结果，无法回溯"哪一步先执行、哪一步后报错"。  
**改进方案**：
- 在 `FlowChart` 中增加播放/暂停/进度条控制，按 SSE 事件的时间戳逐步构建节点
- 每个 step 节点显示实际耗时（已有 `duration_ms` 字段）
- 增加"慢步骤高亮"：耗时 > 阈值（如 2s）的节点边框闪烁提示

**收益**：让调试过程像看视频回放一样直观，快速定位性能瓶颈。

**预估工作量**：2 天

### 9. 答案可信度热力图
**现状**：答案生成后只有引用标记 `[ref:chunk_X]`，用户无法直观看到答案中哪些部分有检索支撑。  
**改进方案**：
- 后端在生成答案时，要求模型对每个句子标注来源 Chunk（已有引用标记基础）
- 前端对答案文本做分句渲染，有引用的句子背景为绿色渐变，无引用的为红色渐变
- 悬停句子时高亮对应的 Chunk 节点

**收益**：直接回答"模型是不是在瞎编"这一核心痛点。

**预估工作量**：2~3 天

### 10. 实验报告自动生成
**现状**：实验结果以 JSON/前端表格呈现，无法导出为可直接写入论文的图表。  
**改进方案**：
- 后端增加 `/api/experiments/report` 接口，返回完整的实验统计（含均值、标准差、置信区间）
- 前端增加"导出报告"按钮，生成 Markdown / PDF 格式的实验报告，内嵌流程图截图与对比表格
- 使用 `html2canvas` + `jspdf` 在前端直接生成 PDF

**收益**：大创/论文结题时可直接使用，极大减少手工整理工作量。

**预估工作量**：2~3 天

---

## 🔵 P4 — 评估体系完善

### 11. 引入自动评估指标（LLM-as-a-Judge）
**现状**：答案质量仅通过"引用是否有效"和"人工标注 alert"间接判断，缺乏对答案本身正确性的评分。  
**改进方案**：
- 增加 `answer_faithfulness`：用本地模型或轻量 LLM 判断答案是否与检索片段一致（如 BERTScore、NLI 模型）
- 增加 `answer_relevance`：判断答案是否直接回应了查询意图
- **成本控制**：使用本地小模型（如 `cross-encoder/nli-deberta-v3-base` 或 ONNX 化的轻量模型），不走 DeepSeek API

**收益**：实验二的"答案质量"指标从"有无引用"升级为"内容真实且相关"，指标更有说服力。

**预估工作量**：3~4 天

### 12. 构建更大规模的标注数据集
**现状**：测试集仅 20 条合成文档 + SQuAD 子集，覆盖度不足。  
**改进方案**：
- 扩展测试查询到 100+ 条，覆盖更多边界 case：
  - 完全无答案（知识库确实没有）
  - 对抗性查询（故意误导性措辞）
  - 超长查询（>500 字符）
  - 跨文档推理（需要组合多个 Chunk 才能回答）
- 人工标注期望 alert 与期望答案，用于计算实验一的 Precision/Recall
- 利用 `generate_poetry_data.py` 的已有脚本能力，批量生成带标注的合成数据

**收益**：实验结果更可靠，论文中的评估章节更有分量。

**预估工作量**：2~3 天（含标注）

---

## 📋 推荐执行路线图

如果是为 **大创结题/课程答辩** 做准备，建议按以下顺序执行：

| 周次 | 任务 | 优先级 | 产出 |
|------|------|--------|------|
| 第 1 周 | 中文支持（P2 #6 #7）+ 混合检索（P0 #1） | P0/P2 | 中文查询可用、hybrid 策略有真实效果 |
| 第 2 周 | 答案可信度热力图（P3 #9）+ 实验报告导出（P3 #10） | P3 | 演示效果爆炸、论文素材直接可用 |
| 第 3 周 | 自动评估指标（P4 #11）+ 数据集扩展（P4 #12） | P4 | 实验数据更扎实、指标更专业 |
| 第 4 周 | 图检索原型（P0 #2）+ 缓存层（P1 #4） | P0/P1 | 复杂查询能力补齐、性能优化 |

如果是为 **长期维护/开源发布** 做准备，建议优先做：
1. 配置管理与 `.env` 清理（安全）
2. 持久化任务队列（可靠性）
3. 图检索原型（技术深度）
4. 完整测试套件（目前项目无任何单元测试，这是开源的最大障碍）

---

## ⚠️ 当前代码中的潜在问题（建议尽快修复）

1. **API Key 泄露风险**：`backend/.env` 中的 `DEEPSEEK_API_KEY` 是真实密钥，且文件未被 `.gitignore` 保护。建议立即轮换密钥，并将 `.env` 加入 `.gitignore`。
2. **无单元测试**：整个后端没有 `pytest` 测试，任何改动都可能破坏已有功能。建议至少为核心服务（`quality.py`、`complexity.py`、`root_cause.py`）补充单元测试。
3. **前端 hardcode API 路径**：`axios` 未配置 baseURL，若部署到非本地环境需手动修改。建议通过 `vite` 的 `server.proxy` 或环境变量处理。
4. **数据库迁移缺失**：使用 SQLAlchemy 创建表但没有 Alembic 迁移脚本，模型变更时难以管理。

---

> 以上方向均可直接作为新需求继续开发。如需深入某个方向，可以指定具体任务，我将给出详细实现方案与代码。
